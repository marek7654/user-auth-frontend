## Sample User Auth App created with React and Bootstrap 5

To run the project, use below commands:

#### `npm install`
#### `npm start`

To configure backend connection set ```src/actions/settings.js``` file.
